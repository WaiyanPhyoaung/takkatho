function makeNumberArray(num1: number): number[] {
  return [num1];
}

function makeStringArray(str1: string): string[] {
  return [str1];
}

function makeArray(item: number | string): (string | number)[] {
  return [item];
}
