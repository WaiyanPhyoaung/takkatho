<div class="homepage-container">
  <section data-animate class="hero-section">
    <!-- Illustration & copy -->
    <div class="hero-inner">
      <svg
        class="uni-icon w-[100px] h-[100px]"
        viewBox="0 0 150 150"
        xmlns="http://www.w3.org/2000/svg"
      >
        <ellipse cx="75" cy="125" rx="50" ry="12"></ellipse>
        <rect x="35" y="60" width="80" height="50" rx="8"></rect>
        <rect x="55" y="85" width="40" height="25" rx="4"></rect>
        <polygon points="75,30 25,60 125,60"></polygon>
        <rect x="68" y="95" width="14" height="15" rx="2"></rect>
        <rect x="40" y="65" width="10" height="10" rx="2"></rect>
        <rect x="100" y="65" width="10" height="10" rx="2"></rect>
        <rect x="40" y="80" width="10" height="10" rx="2"></rect>
        <rect x="100" y="80" width="10" height="10" rx="2"></rect>
      </svg>

      <h1>Learn. Build.<br />Innovate.</h1>
      <p>
        Discover a world of university-level courses curated by leading
        academics and industry experts. Join thousands of students advancing
        their careers through practical education.
      </p>

      <div class="hero-actions">
        <a class="btn-primary" href="/websocket/introduction/what-is-websocket">
          Start Learning
          <svg
            viewBox="0 0 24 24"
            width="24"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M13.75 6.75L19.25 12L13.75 17.25"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"></path>
            <path
              d="M19 12H4.75"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"></path>
          </svg>
        </a>
        <a class="btn-glass" href="/about">About Us</a>
      </div>
    </div>
  </section>

  <!-- ── Featured Courses ─────────────────── -->
  <section data-animate class="featured-section">
    <h2>Featured Courses</h2>

    <div class="cards">
      <!-- Single card (Computer Science) -->
      <article class="card card-glass">
        <header>
          <span class="icon cs"></span>
          <h3>Computer Science</h3>
        </header>
        <span class="pill blue">Beginner&nbsp;→&nbsp;Advanced</span>
        <p>
          Explore algorithms, data structures, and programming fundamentals with
          our comprehensive computer science curriculum.
        </p>
        <a class="card-link" href="/websocket/introduction/what-is-websocket">
          View Course
          <svg
            viewBox="0 0 20 20"
            width="20"
            xmlns="http://www.w3.org/2000/svg"
          >
            <path
              d="M4.17 10H15.83M10 4.17L15.83 10 10 15.83"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"></path>
          </svg>
        </a>
      </article>

      <!-- … replicate for Data Science & Web Dev (omitted for brevity) … -->
    </div>
  </section>

  <!-- ── Benefits ─────────────────────────── -->
  <section data-animate class="benefits-section">
    <h2>Why Choose UniCourses?</h2>
    <div class="benefit-list">
      <!-- item -->
      <div class="benefit">
        <span class="benefit-icon expert"></span>
        <h3>Expert Instructors</h3>
        <p>
          Learn from professors and industry professionals with years of
          experience in their fields.
        </p>
      </div>
      <!-- item -->
      <div class="benefit">
        <span class="benefit-icon flexible"></span>
        <h3>Flexible Learning</h3>
        <p>
          Study at your own pace with on-demand lectures, interactive
          assignments, and flexible deadlines.
        </p>
      </div>
      <!-- item -->
      <div class="benefit">
        <span class="benefit-icon career"></span>
        <h3>Career Advancement</h3>
        <p>
          Gain practical skills and credentials that employers value to advance
          your career opportunities.
        </p>
      </div>
    </div>
  </section>

  <!-- ── Community ────────────────────────── -->
  <section data-animate class="community-section">
    <h2>Join Our Academic Community</h2>
    <p>
      Connect with fellow learners, participate in discussions, and collaborate
      on projects in our vibrant academic community.
    </p>
    <a class="btn-primary" href="/websocket/introduction/what-is-websocket">
      Explore Courses
      <svg viewBox="0 0 20 20" width="20" xmlns="http://www.w3.org/2000/svg">
        <path
          d="M10 17.5L8.94 16.44 14.12 11.25H2.5V9.75H14.12L8.94 4.56 10 3.5 17.5 11 10 17.5Z"
          fill="currentColor"></path>
      </svg>
    </a>
  </section>
</div>

<!-- ───────────────────── CSS ───────────────────── -->
<style is:inline>
  :root {
    --purple: #4f46e5;
    --purple-dark: #312e81;
    --txt-muted: #888;
    --glass-bg: rgba(255, 255, 255, 0.03);
  }

  .homepage-container {
    font-family: ui-sans-serif, system-ui, sans-serif;
    line-height: 1.6;
    color: #fff;
  }

  /* Hero */
  .hero-section {
    position: relative;
    overflow: hidden;
    min-height: 80vh;
    padding: 4rem 1.5rem 6rem;
    background: linear-gradient(to bottom right, #000, #111);
  }
  .hero-bg path {
    fill: var(--purple);
  }
  .hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 220px;
    z-index: 0;
  }
  .hero-inner {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 900px;
    margin: 0 auto;
  }
  .uni-icon ellipse {
    fill: #c7d2fe;
    opacity: 0.7;
  }
  .uni-icon rect,
  .uni-icon polygon {
    fill: var(--purple);
  }
  h1 {
    font-size: clamp(2.5rem, 6vw, 5rem);
    font-weight: 800;
    margin: 1rem 0 1.5rem;
    background: linear-gradient(to right, #fff 20%, #666 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    letter-spacing: -0.05em;
    line-height: 1.1;
  }
  p {
    color: var(--txt-muted);
    max-width: 800px;
    margin: 0 auto 3rem;
  }

  /* Buttons */
  .btn-primary,
  .btn-glass {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    border-radius: 0.75rem;
    font-weight: 600;
    text-decoration: none;
    transition: 0.3s all;
  }
  .btn-primary {
    background: linear-gradient(to right, #0070f3, #00a6ff);
    box-shadow: 0 4px 14px rgba(0, 118, 255, 0.39);
  }
  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 118, 255, 0.23);
  }
  .btn-glass {
    border: 1px solid rgba(255, 255, 255, 0.3);
    backdrop-filter: blur(10px);
  }
  .btn-glass:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.5);
  }

  /* Featured */
  .featured-section {
    background: #111;
    padding: 6rem 2rem;
    position: relative;
  }
  .featured-section::before {
    content: "";
    position: absolute;
    inset: 0;
    height: 1px;
    top: 0;
    background: linear-gradient(
      to right,
      transparent,
      rgba(255, 255, 255, 0.1),
      transparent
    );
  }
  .featured-section h2 {
    font-size: 3rem;
    font-weight: 800;
    text-align: center;
    margin-bottom: 2rem;
  }
  .cards {
    display: grid;
    gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    max-width: 1200px;
    margin: 0 auto;
  }
  .card {
    border-radius: 1rem;
    padding: 2rem;
    transition:
      0.4s transform,
      0.4s box-shadow,
      0.4s background,
      0.4s border-color;
    cursor: pointer;
  }
  .card-glass {
    background: var(--glass-bg);
    border: 1px solid rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
  }
  .card-glass:hover {
    transform: translateY(-8px);
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.2);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
  }
  .card header {
    display: flex;
    align-items: center;
    margin-bottom: 1.5rem;
  }
  .icon {
    width: 48px;
    height: 48px;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 1rem;
  }
  .icon.cs {
    background: linear-gradient(135deg, #0070f3 0%, #00a6ff 100%);
    box-shadow: 0 8px 16px rgba(0, 118, 255, 0.2);
  }
  .pill {
    display: inline-block;
    padding: 0.5rem 1rem;
    border-radius: 9999px;
    font-size: 0.875rem;
    font-weight: 500;
    border: 1px solid transparent;
  }
  .pill.blue {
    background: rgba(0, 118, 255, 0.1);
    color: #0070f3;
    border-color: rgba(0, 118, 255, 0.2);
  }
  .card-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: #0070f3;
    font-weight: 600;
    text-decoration: none;
    transition: 0.3s all;
  }
  .card-link:hover {
    color: #00a6ff;
    transform: translateX(4px);
  }

  /* Benefits */
  .benefits-section {
    background: linear-gradient(135deg, #ede9fe 0%, #f5f3ff 100%);
    border-radius: 1rem;
    padding: 3rem 2rem;
    margin: 6rem 0;
    text-align: center;
    color: #4c1d95;
  }
  .benefits-section h2 {
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 2rem;
  }
  .benefit-list {
    display: grid;
    gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    color: #6b7280;
  }
  .benefit {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .benefit-icon {
    width: 48px;
    height: 48px;
    border-radius: 24px;
    margin-bottom: 1rem;
  }
  /* (SVG icons omitted for brevity; keep the originals if you like) */

  /* Community */
  .community-section {
    text-align: center;
    margin-bottom: 6rem;
    color: #312e81;
  }
  .community-section h2 {
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }
  .community-section p {
    color: #4b5563;
    max-width: 700px;
    margin: 0 auto 2rem;
  }

  /* Fade-in utility */
  [data-animate] {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.6s ease-out;
  }
  .animate-fade-in {
    opacity: 1 !important;
    transform: none !important;
  }

  @media (max-width: 768px) {
    .hero-section {
      padding: 2rem 1rem;
      min-height: 60vh;
    }
    h1 {
      font-size: 2.25rem;
    }
    p {
      font-size: 1rem;
    }
  }
</style>

<!-- ──────────────────── JS ──────────────────── -->
<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    // 1) Initial fade-in for the top hero
    document.querySelector(".hero-section").classList.add("animate-fade-in");

    // 2) Lazy fade-in for the rest
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("animate-fade-in");
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1 }
    );

    document.querySelectorAll("[data-animate]").forEach((el) => {
      if (el !== document.querySelector(".hero-section")) observer.observe(el);
    });
  });
</script>
